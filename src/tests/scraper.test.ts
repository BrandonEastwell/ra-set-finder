

describe('scrapes artist names from resident advisor event page', () => {
  it('should return names of artist', () => {
    
  });

  it('should find artist names from lineup header', () => {
    
  });
});